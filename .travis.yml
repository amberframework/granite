language: generic

services:
  - docker

before_install:
  - docker-compose build spec

script:
  - docker-compose run spec

env:
  - CURRENT_ADAPTER=mysql
  - CURRENT_ADAPTER=pg
  - CURRENT_ADAPTER=sqlite
  - PG_VERSION=10.4
  - PG_VERSION=10.5
  - MYSQL_VERSION=5.6
  - MYSQL_VERSION=5.5
